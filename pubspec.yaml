name: savings_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.1.5
  fl_chart: ^0.66.0
  intl: ^0.18.1
  #isar: ^3.1.0+1
  #isar_flutter_libs: ^3.1.0+1 # Zawiera natywne biblioteki dla platform
  path_provider: ^2.0.0
  shared_preferences: ^2.2.0
  image_picker: ^1.2.1
  image_cropper: ^11.0.0
  image: ^4.0.15
  google_mlkit_text_recognition: ^0.15.0
  # Drift (SQLite) - lightweight local SQL with codegen
  drift: ^2.0.0
  sqlite3_flutter_libs: ^0.5.0
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.7
  drift_dev: ^2.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

flutter_launcher_icons:
  image_path: "assets/app_icon.png"
  android: true
  ios: true